---
controller_templates:
  - name: SERVER / RHEL7 - Register
    project: Automated Management
    playbook: server_register.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    limit: rhel7
    extra_vars:
      org_id: "Default_Organization"
      rhsm_enabled_repos:
        - rhel-7-server-rpms
        - rhel-7-server-extras-rpms
        - rhel-7-server-satellite-client-6-rpms
      include_offset_task_delay: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Server Name or Pattern
          type: text
          variable: PREFIX
          required: false
        - question_name: Choose Environment
          type: multiplechoice
          variable: env
          choices:
            - Dev
            - QA
            - Prod
          required: true

  - name: SERVER / RHEL7 - Register via Satellite API
    project: Automated Management
    playbook: server_register_via_api.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
      - Satellite Credential
    limit: localhost,rhel7
    extra_vars:
      el_distribution: RHEL7
      organization: "Default Organization"
      location: "Default Location"
      rhsm_enabled_repos:
        - rhel-7-server-rpms
        - rhel-7-server-extras-rpms
        - rhel-7-server-satellite-client-6-rpms
      include_offset_task_delay: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Server Name or Pattern
          type: text
          variable: PREFIX
          required: false
        - question_name: Choose Environment
          type: multiplechoice
          variable: env
          choices:
            - Dev
            - QA
            - Prod
          required: true

  - name: SERVER / CentOS7 - Register via Satellite API
    project: Automated Management
    playbook: server_register_via_api.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
      - Satellite Credential
    limit: localhost,centos7
    extra_vars:
      el_distribution: CentOS7
      organization: "Default Organization"
      location: "Default Location"
      include_offset_task_delay: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Server Name or Pattern
          type: text
          variable: PREFIX
          required: false
        - question_name: Choose Environment
          type: multiplechoice
          variable: env
          choices:
            - Dev
            - QA
            - Prod
          required: true

  - name: SERVER / CentOS7 - Register
    project: Automated Management
    playbook: server_register.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    limit: centos7
    extra_vars:
      org_id: "Default_Organization"
      product_name_or_regex: "Community Enterprise OS 7"
      include_offset_task_delay: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Server Name or Pattern
          type: text
          variable: PREFIX
          required: false
        - question_name: Choose Environment
          type: multiplechoice
          variable: env
          choices:
            - Dev
            - QA
            - Prod
          required: true

  - name: SATELLITE / RHEL - Publish Content View
    project: Automated Management
    playbook: satellite_publish.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    extra_vars:
      env: Dev
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select Content View
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list}}"
          required: true

  - name: SATELLITE / CentOS - Publish Content View
    project: Automated Management
    playbook: satellite_publish.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    extra_vars:
      env: Dev
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select Content View
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list}}"
          required: true

  - name: SATELLITE / RHEL - Promote Content View
    project: Automated Management
    playbook: satellite_promote.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select Content View
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list}}"
          required: true
        - question_name: Current Lifecycle Environment
          variable: current_lifecycle_environment
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list}}"
          required: true
        - question_name: Next Lifecycle Environment
          variable: lifecycle_environment
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list}}"
          required: true

  - name: SATELLITE / CentOS - Promote Content View
    project: Automated Management
    playbook: satellite_promote.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select Content View
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list}}"
          required: true
        - question_name: Current Lifecycle Environment
          variable: current_lifecycle_environment
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list}}"
          required: true
        - question_name: Next Lifecycle Environment
          variable: lifecycle_environment
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list}}"
          required: true  

  - name: SATELLITE / Delete host(s) in "node_group"
    project: Automated Management
    playbook: satellite_delete_host.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    extra_vars:
      node_group: env_CentOS7_Dev
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SATELLITE / Refresh manifest
    project: Automated Management
    playbook: satellite_manifest_refresh.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SATELLITE / Resume paused tasks
    project: Automated Management
    playbook: satellite_hammer_task_resume.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SATELLITE / Resource Info
    project: Automated Management
    playbook: query_satellite_resource_info_survey.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Satellite resource info type
          type: multiplechoice
          variable: resource_info_survey
          choices:
            - organizations
            - locations
            - subscriptions
            - capsules
            - hosts
            - content_views
            - lifecycle_environments
            - activation_keys
            - repositories
            - list_all_options
          required: true

  - name: SATELLITE / Performance Tuning
    project: Automated Management
    playbook: setup_satellite_tuning.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SERVER / Register 
    project: Automated Management
    playbook: server_register_dynamic_inv.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      org_id: Default_Organization
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true

  - name: SERVER / Unregister 
    project: Automated Management
    playbook: server_unregister.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      org_id: Default_Organization
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true

  - name: OS / Patch OS to latest
    project: Automated Management
    playbook: server_patch.yml
    inventory: EC2 Dynamic Inventory
    use_fact_cache: true
    credentials:
      - Workshop Credential
    extra_vars:
      include_startup_delay: true
    ask_variables_on_launch: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['all_lifecycle_environments'] | selectattr('name', 'match', 'all') | map(attribute='name') }}"
          required: true
        - question_name: Include kernel updates? (Kernel update will result in system reboot)
          type: multiplechoice
          variable: include_kernel_updates
          choices:
            - 'yes'
            - 'no'
          default: 'yes'
          required: false
        - question_name: Check for updates only? Do not patch at this time?
          type: multiplechoice
          variable: update_check_only
          choices:
            - 'yes'
            - 'no'
          default: 'no'
          required: false
    ask_limit_on_launch: true

  - name: SERVER / Fact Scan
    project: Fact Scan
    playbook: scan_facts.yml
    inventory: EC2 Dynamic Inventory
    use_fact_cache: true
    credentials:
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true

  - name: SERVER / Start node via ec2_node_tools
    description: Start node - selection via EC2 instance tags
    project: Automated Management
    playbook: node_startup.yml
    inventory: Workshop Inventory
    limit: control
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SERVER / Stop node via ec2_node_tools
    description: Stop node - selection via EC2 instance tags
    project: Automated Management
    playbook: node_shutdown.yml
    inventory: Workshop Inventory
    limit: control
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: UTILITY / Snapshot Instance
    description: Node snapshot actions - selection via dynamic inventory group
    project: Automated Management
    playbook: snapshot_instance_lvm.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      snapshot_set_name: snap01
      snapshot_create_snapshot_autoextend_threshold: 70
      snapshot_create_snapshot_autoextend_percent: 20
      snapshot_create_boot_backup: true
      snapshot_create_volumes:
        - vg: VolGroup00
          lv: rootVol
          size: 3G
        - vg: VolGroup00
          lv: varVol
          size: 5G
        - vg: VolGroup00
          lv: logVol
          size: 500M
        - vg: VolGroup00
          lv: auditVol
          size: 100M
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
        - question_name: Snapshot action
          type: multiplechoice
          variable: lvm_snapshots_action
          choices:
            - check
            - create
            - revert
            - remove
          required: true
    ask_variables_on_launch: true
    ask_limit_on_launch: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: UTILITY / Expand logical volume and associated filesystem
    description: Expand logical volume - selection via dynamic inventory group
    project: Automated Management
    playbook: extend_lvm.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      volume_group_name: "VolGroup00"
      logical_volume_name: "varVol"
      extend_logical_volume_mb: 1024
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_variables_on_launch: true
    ask_limit_on_launch: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: UTILITY / Clear yum cache
    description: Take node snapshot - selection via dynamic inventory group
    project: Automated Management
    playbook: clear_yum_cache.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: UTILITY / Register OracleLinux7 node
    description: Register OracleLinux7 node to Satellite
    project: Automated Management
    playbook: c2r_research.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      satellite_organization: 'Default_Organization'
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_variables_on_launch: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: OS / Pre-conversion Analysis
    description: Pre-conversion analysis of EL systems for RHEL7 conversion target
    project: Automated Management
    playbook: c2r_analysis.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      org_id: "Default_Organization"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select EL Group to analyze
          question_description: Use to limit job to hosts that are members of the selected host group.
          variable: dynamic_inventory_group
          default: ''
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') | list }}"
          required: true
        - question_name: Select RHEL Lifecycle Environment to use for analysis
          question_description: Select RHEL Lifecycle Environment to use during ConvertRHEL pre-conversion analysis.
          variable: rhel_lifecycle_environment
          default: ''
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS7_to_RHEL7.*$') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true

  - name: OS / Convert to RHEL7
    description: Convert EL OS to RHEL OS via targetted LE
    project: Automated Management
    playbook: c2r_convert.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      org_id: "Default_Organization"
      convert_convert2rhel_opts: '--no-rpm-va'
      convert_reboot_requested: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select EL Group to convert
          question_description: Use to limit job to selected inventory host group.
          variable: dynamic_inventory_group
          type: multiplechoice
          default: ''
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') | list }}"
          required: true
        - question_name: Select target RHEL Lifecycle Environment to convert to
          question_description: Specify RHEL Lifecycle Environment for conversion RPM package sources.
          variable: rhel_lifecycle_environment
          type: multiplechoice
          default: ''
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') | list + hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS7_to_RHEL7.*$') | map(attribute='name') | list }}"
          required: true
    ask_variables_on_launch: true
    ask_limit_on_launch: true

  - name: CONVERT2RHEL / 98 - Three Tier App deployment
    description: Deploy three tier app
    project: Three Tier App
    playbook: Configure_3TA_AWS_research.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      pg_db_name: db01
      pg_db_user: user01
      pg_db_passwd: Xtr4Sup3rS3cr3tP4ssw0rd?!
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Choose application stack name
          question_description: Selection of specific application stack name.
          type: multiplechoice
          variable: app_stack_name
          choices:
            - stack01
            - stack02
          required: true

  - name: CONVERT2RHEL / 99 - Three Tier App smoke test
    description: Smoke test three tier app
    project: Three Tier App
    playbook: SmokeTest_3TA_AWS.yml
    inventory: EC2 Dynamic Inventory
    execution_environment: three tier app execution environment
    credentials:
      - Workshop Credential
    extra_vars:
      pg_db_name: db01
      pg_db_user: user01
      pg_db_passwd: Xtr4Sup3rS3cr3tP4ssw0rd?!
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Choose application stack name
          question_description: Selection of specific application stack name.
          type: multiplechoice
          variable: app_stack_name
          choices:
            - stack01
            - stack02
          required: true

  - name: ZZ / UTILITY / Take EC2 EBS snapshot of node
    description: Take node snapshot - selection via EC2 instance tags
    project: Automated Management
    playbook: snapshot_instance_ec2.yml
    inventory: Workshop Inventory
    limit: control
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          short_name: node*
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: ZZ / UTILITY / Revert node EC2 EBS snapshot
    description: Revert snapshot - selection via EC2 instance tags
    project: Automated Management
    playbook: snapshot_revert_ec2.yml
    inventory: Workshop Inventory
    limit: control
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          username: ec2-user
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: ZZ / UTILITY / Delete node EC2 EBS snapshot
    description: Delete snapshot - selection via EC2 snapshot tags
    project: Automated Management
    playbook: snapshot_delete_ec2.yml
    inventory: Workshop Inventory
    limit: control
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      snapshots:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Set instance tags based on Satellite facts
    description: Set instance tags - selection via existing EC2 instance tags
    project: Automated Management
    playbook: query_satellite_resource_info_hosts_set_instance_tags.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Set instance tag - RHEL
    description: Set instance tag - app_stack_name and groups provided via extra_vars dict
    project: Automated Management
    playbook: set_instance_tags.yml
    inventory: Workshop Inventory
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
      group_tag_map:
        node1.example.com: frontends
        node2.example.com: apps
        node3.example.com: appdbs
      app_stack_name: stack01
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Set instance tag - CentOS
    description: Set instance tag - app_stack_name and groups provided via extra_vars dict
    project: Automated Management
    playbook: set_instance_tags.yml
    inventory: Workshop Inventory
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
      group_tag_map:
        node4.example.com: frontends
        node5.example.com: apps
        node6.example.com: appdbs
      app_stack_name: stack02
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Set instance tag - OracleLinux7
    description: Set instance tag - app_stack_name and groups provided via extra_vars dict
    project: Automated Management
    playbook: set_instance_tags_ol7.yml
    inventory: Workshop Inventory
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
      environment_tag_map:
        node4.example.com: Dev
        node5.example.com: Dev
        node6.example.com: Dev
      group_tag_map:
        node4.example.com: frontends
        node5.example.com: apps
        node6.example.com: appdbs
      app_stack_name: stack02
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Get instance details via ec2_node_tools role
    description: Utilize ec2_node_tools role to display info available per instance/node
    project: Automated Management
    playbook: node_details_via_ec2_node_tools.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
          Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
          short_name: node*
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: EC2 / Update Route53
    description: Update Route53 records with external IPs
    project: Automated Management
    playbook: refresh_route53_dns.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          Workshop: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
      student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
      workshop_dns_zone: "{{ lookup('env', 'AWX_HOST').split('.',2)[2] }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: CONTROLLER / Update inventories via dynamic sources
    project: Automated Management
    playbook: controller_inventory_source_update.yml
    inventory: Workshop Inventory
    credentials:
      - Controller Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory to update
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'defined') | map(attribute='name') | list}}"
          required: true
        - question_name: Choose Environment
          type: multiplechoice
          variable: env
          choices:
            - Dev
            - QA
            - Prod
          required: true

  - name: Z / CaC / Satellite
    project: Automated Management
    playbook: setup_satellite.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      refresh_satellite_manifest: false
      bulk_load_size: 1000
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    ask_variables_on_launch: true

  - name: Z / SETUP / Cockpit - Web Console
    description: Install and configure Cockpit Web Console for all nodes
    project: Automated Management
    playbook: setup_cockpit.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      instances:
        tags:
          Workshop: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
      student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
      workshop_dns_zone: "{{ lookup('env', 'AWX_HOST').split('.',2)[2] }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: Z / INSIGHTS / Replace Satellite Manifest
    project: Automated Management
    playbook: insights_replace_satellite_manifest.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      manifest_name: smrtmgmt-wrksp
      offline_token: value
      rhsm_org_id: value
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: Z / INSIGHTS / Install and Register
    project: Automated Management
    playbook: insights_install.yml
    inventory: Workshop Inventory
    limit: rhel
    credentials:
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: Z / INSIGHTS / Create Insights Credential
    project: Automated Management
    playbook: insights_setup_aap_creds.yml
    execution_environment: Insights execution environment
    inventory: Workshop Inventory
    credentials:
      - Controller Credential
      - Workshop Credential
    extra_vars:
      redhat_portal_username: username
      redhat_portal_password: password
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: Z / INSIGHTS / Create Insights Project
    project: Automated Management
    playbook: insights_create_insights_project.yml
    execution_environment: Insights execution environment
    inventory: Workshop Inventory
    credentials:
      - Controller Credential
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: OS / Leapp / Remediation
    project: Automated Management
    playbook: leapp_remediate.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: true

  - name: OS / Convert2RHEL / Post-Remediation
    description: Perform post conversion OS remediations for three tier app
    project: Three Tier App
    playbook: ConversionPostgres_3TA_AWS_research.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Choose application stack name
          question_description: Selection of specific application stack name.
          type: multiplechoice
          variable: app_stack_name
          choices:
            - stack01
            - stack02
          required: true

  - name: Z / CaC / Satellite / C2R - RIPU scenario
    project: Automated Management
    playbook: setup_satellite_via_varsfiles.yml
    inventory: Workshop Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    extra_vars:
      refresh_satellite_manifest: false
    ask_variables_on_launch: true
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Choose RHEL conversion-upgrade scenario to prepare on Satellite
          type: multiplechoice
          variable: satellite_cac_scenario
          choices:
            - CENTOS7_to_RHEL7
            - ORACLE7_to_RHEL7
            - RHEL7_to_RHEL8
            - RHEL8_to_RHEL9
          required: true

  - name: Z / CaC / Controller / RIPU scenario
    project: Automated Management
    playbook: setup_controller_via_varsfiles.yml
    inventory: Workshop Inventory
    credentials:
      - Controller Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    extra_vars:
      controller_configuration_async_retries: 72
      controller_configuration_async_delay: 5
    ask_variables_on_launch: true
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Choose C2R/RIPU version to prepare on Controller
          question_description: Select Convert2RHEL, RHEL8, or RHEL9 CaC scenario
          type: multiplechoice
          variable: controller_cac_scenario
          choices:
            - CentOS7_C2R
            - RHEL8
            - RHEL9
          default: ''
          required: true

  - name: UTILITY / Extend partition and resize physical volume
    project: Automated Management
    playbook: partition_lvm_resize.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    ask_limit_on_launch: true
    ask_variables_on_launch: true
    extra_vars:
      full_storage_device_name: /dev/nvme0n1
      perform_resize: true

  - name: UTILITY / Extend logical volumes and filesystems
    project: Automated Management
    playbook: filesystem_extend.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          question_description: Use to limit job to selected inventory host group.
          type: multiplechoice
          default: ''
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['all_lifecycle_environments'] | selectattr('name', 'match', 'all') | map(attribute='name') }}"
          required: true
    ask_limit_on_launch: true
    ask_variables_on_launch: true
    extra_vars:
      filesystem_extend_volumes:
        - vg: VolGroup00
          lv: varVol
          size: 8G

  - name: Z / SETUP / Miscellaneous host config
    project: Automated Management
    playbook: misc_host_config.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list + [ 'aws_ec2' ] }}"
          required: true
    extra_vars: ''
    ask_limit_on_launch: true

  - name: Z / SETUP / CentOS system mods
    project: Automated Management
    playbook: centos_system_mods.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    survey_enabled: false
    extra_vars: ''
    ask_limit_on_launch: true

  - name: Z / SETUP / Disable transparent hugepages
    project: Automated Management
    playbook: disable_transparent_hugepages.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      HOSTS: satellite.example.com

  - name: UTILITY / Workflow node delay
    project: Automated Management
    playbook: workflow_node_delay.yml
    inventory: Workshop Inventory
    credentials:
      - Controller Credential
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    extra_vars:
      workflow_node_delay_in_seconds: 30
    ask_variables_on_launch: false
    survey_enabled: false

  - name: EC2 / Instance action
    description: Perform EC2 instance actions - instance details, start, or stop
    project: Automated Management
    playbook: ec2_instance_action.yml
    inventory: Workshop Inventory
    credentials:
      - Workshop Credential
    extra_vars:
      ec2_region: "{{ workshop_ec2_region }}"
      x_vars_guid: "{{ lookup('env', 'AWX_HOST' ).split('.')[1] }}"
      x_vars_Student: "{{ lookup('env', 'AWX_HOST').split('.')[0] | urlsplit('hostname') }}"
    ask_variables_on_launch: true
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select EC2 instance action
          type: multiplechoice
          variable: instance_action
          choices:
            - instance_details
            - start
            - stop
          required: true
        - question_name: Select OS target
          variable: content_view
          type: multiplechoice
          choices: "{{ hostvars['satellite.example.com']['satellite_content_views'] | selectattr('name', 'defined') | map(attribute='name') | list}}"
          required: true
        - question_name: Select Environment stage
          type: multiplechoice
          variable: stage
          choices:
            - Dev
            - QA
            - Prod
          required: true
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry

  - name: SATELLITE / Query for content host configuration details
    project: Automated Management
    playbook: query_satellite_resource_info_hosts_display_content_details.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      org_name: "{{ hostvars['satellite.example.com']['satellite_organization'] }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          question_description: Use to limit job to selected inventory host group.
          type: multiplechoice
          default: ''
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
        - question_name: Enter list of specific content hosts to limit to delimited by commas
          question_description: Use to limit job to specified content hosts that are members of inventory host group, leave blank for no limit.
          type: text
          default: ''
          variable: inventory_host_limit
          required: false
    ask_limit_on_launch: false

  - name: SATELLITE / Change content source for content host
    project: Automated Management
    playbook: satellite_node_change_content_source.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Satellite Credential
      - Workshop Credential
    extra_vars:
      org_name: "{{ hostvars['satellite.example.com']['satellite_organization'] }}"
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          question_description: Use to limit job to selected inventory host group.
          type: multiplechoice
          default: ''
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
        - question_name: Enter list of specific content hosts to limit to delimited by commas
          question_description: Use to limit job to specified content hosts that are members of inventory host group, leave blank for no limit.
          type: text
          default: ''
          variable: inventory_host_limit
          required: false
        - question_name: Select target lifecycle environment for content host
          question_description: Lifecycle environment for content host to be changed to.
          type: multiplechoice
          default: ''
          variable: target_lifecycle_environment
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '.*') | map(attribute='name') | list }}"
          required: true
    ask_limit_on_launch: false

  - name: SERVER / Update content host system facts on Satellite
    project: Automated Management
    playbook: subman_facts_update.yml
    inventory: EC2 Dynamic Inventory
    credentials:
      - Workshop Credential
    ask_variables_on_launch: false
    notification_templates_started: Telemetry
    notification_templates_success: Telemetry
    notification_templates_error: Telemetry
    survey_enabled: true
    survey:
      name: ''
      description: ''
      spec:
        - question_name: Select inventory group
          type: multiplechoice
          variable: dynamic_inventory_group
          choices: "{{ hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^RHEL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^CentOS.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['satellite_lifecycle_environments'] | selectattr('name', 'match', '^OL.*$') | map(attribute='name') +
                       hostvars['satellite.example.com']['all_lifecycle_environments'] | selectattr('name', 'match', 'all') | map(attribute='name') }}"
          required: true
    ask_limit_on_launch: true

  - name: Demo Job Template
    state: absent
...
